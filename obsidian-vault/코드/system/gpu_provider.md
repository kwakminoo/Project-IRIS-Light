# gpu_provider

`iris/system/gpu_provider.py`

GPU 사용률 수집 — nvidia-smi → Windows PDH → N/A.

## 주요 정의

- `def read_gpu_utilization`
- `def _read_nvidia_smi`
- `def _read_windows_pdh_gpu`
- `def _pdh_query_avg`
